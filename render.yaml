services:
  - type: web
    name: dostxamcen-website
    env: node
    plan: free
    nodeVersion: 20
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
